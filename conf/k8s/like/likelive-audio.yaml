apiVersion: apps/v1
kind: Deployment
metadata:
  name: like-live-audio-consumer
  namespace: like
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 6
  selector:
    matchLabels:
      app: like-live-audio-consumer
  template:
    metadata:
      labels:
        app: like-live-audio-consumer
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      affinity:
         nodeAffinity:
           requiredDuringSchedulingIgnoredDuringExecution:
             nodeSelectorTerms:
             - matchExpressions:
               - key: audio
                 operator: In
                 values:
                 - "true"
         podAntiAffinity:
           requiredDuringSchedulingIgnoredDuringExecution:
           - labelSelector:
               matchExpressions:
               - key: service-type
                 operator: In
                 values:
                 - like-live-audio
             topologyKey: kubernetes.io/hostname
      restartPolicy: Always
      imagePullSecrets:
      - name: harbor
      terminationGracePeriodSeconds: 3
      containers:
      - name: consumer
        resources:
          limits:
            cpu: 18       # Required
            memory: 28Gi # Required
          requests:
            cpu: 12
            memory: 14G
        imagePullPolicy: Always
        image: __IMAGE__
        command:
        - "./kafka-consumer-cpp"
        - "-usercode_in_pthread=true"
        - "-connect_timeout_as_unreachable=20"
        - "-health_check_interval=1"
        - "-config_file=/app/runtime/likelive-audio.conf"

        livenessProbe:
          httpGet: 
            path: /
            port: 8000                                           # Required
          initialDelaySeconds: 5
          failureThreshold: 3
          timeoutSeconds: 3
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8000                                            # Required
          initialDelaySeconds: 5
          timeoutSeconds: 3
          periodSeconds: 10
        volumeMounts:     #挂载volumes中定义的磁盘
        - name: data
          mountPath: /data
        - name: tmp
          mountPath: /tmp
        - name: config
          mountPath: /app/runtime
      terminationGracePeriodSeconds: 3
      volumes:
      - name: data
        hostPath:
          path: /data
      - name: tmp
        hostPath:
          path: /tmp
      - name: config
        configMap:
          name: like-live-audio-consumer

---

apiVersion: v1
kind: Service
metadata:
  labels:
    app: like-live-audio-consumer
  name: like-live-audio-consumer
  namespace: like
spec:
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  selector:
    app: like-live-audio-consumer

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: like-live-audio-consumer
    release: pga
  name: like-live-audio-consumer
  namespace: like
spec:
  endpoints:
  - interval: 5s
    path: /brpc_metrics
    port: http
    relabelings:
    - sourceLabels: [__meta_kubernetes_endpoint_node_name]
      separator: ;
      regex: (.*)
      targetLabel: node
      replacement: $1
      action: replace

  jobLabel: like-live-audio-consumer
  namespaceSelector:
    matchNames:
    - like
  selector:
    matchLabels:
      app: like-live-audio-consumer
